/* Consolidated replacement: legacy DOM injection removed. Functional modules own their own UI. */
(() => {
  'use strict';
  // Intentionally empty: Questões, Ações and Admin integration have dedicated modules.
})();
